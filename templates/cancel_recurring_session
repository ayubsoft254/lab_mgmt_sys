{% extends 'base.html' %}

{% block content %}
<div class="container mx-auto p-6 animate-fade-in">
    <h1 class="text-3xl font-bold text-ttu-dark mb-6">Cancel Recurring Session</h1>

    <div class="glass-card p-6">
        <h2 class="text-2xl font-semibold text-ttu-dark mb-4">Are you sure you want to cancel this recurring session?</h2>

        <div class="mb-4 text-ttu-dark">
            <p><strong>Lab:</strong> {{ recurring_session.lab.name }}</p>
            <p><strong>Title:</strong> {{ recurring_session.title }}</p>
            <p><strong>Recurrence:</strong> {{ recurring_session.get_recurrence_type_display }}</p>
            <p><strong>Start Date:</strong> {{ recurring_session.start_date|date:"F j, Y" }}</p>
            <p><strong>End Date:</strong> {{ recurring_session.end_date|date:"F j, Y" }}</p>
        </div>

        <div class="bg-ttu-light border-l-4 border-ttu-yellow text-ttu-dark p-4 mb-4 rounded">
            <p><strong>Warning:</strong> This will delete all future sessions associated with this recurring booking.</p>
        </div>

        <form method="post">
            {% csrf_token %}
            <div class="flex items-center">
                <button type="submit" class="btn-danger mr-4">
                    Confirm Cancel
                </button>
                <a href="{% url 'recurring_sessions_list' %}" class="btn-outline-ttu">
                    Back to Recurring Sessions
                </a>
            </div>
        </form>
    </div>
</div>
{% endblock %}